# ---- Secrets (NEVER commit) ----
# Section 5.4 / 12: cookies and tokens must not be Git-managed.
cookies.txt
*.cookies.txt
secrets/
*.pem
*.key
.env
.env.*
!.env.example
!.env.production.example

# ---- Google Takeout exports (personal data) ----
*.zip
takeout*.zip
takeout-*.zip
takeout_imports/
takeout/
/takeout/

# ---- Backups / rehearsal artifacts (Phase 9F: dumps, manifests, reports) ----
backups/
/backups/

# ---- Release artifacts (Phase 10A: manifests, SBOMs, scans — NOT committed) ----
release/
/release/

# ---- Archived media / logs / runtime data (rooted and anywhere) ----
/archive/
/logs/
/config/
/data/
archive/
logs/
config/
data/
*.sqlite3
*.db

# ---- Local Docker credential workaround (Mac: Docker Desktop/OrbStack mix) ----
.docker-config-local/

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
youtube_archiver.egg-info/
.eggs/
build/
dist/
# Frontend (Phase 5A)
frontend/node_modules/
frontend/dist/
frontend/.vite/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- OS ----
.DS_Store
