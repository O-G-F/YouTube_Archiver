# Pinned-ish runtime dependencies for reproducible Docker builds.
# The package itself is installed via `pip install -e .` (see Dockerfile),
# this file exists so the heavy dependency layer can be cached separately.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
# Phase 10B.2: use the SOURCE distribution `psycopg2` (built into a wheel in a
# dedicated builder stage), NOT `psycopg2-binary`. The -binary manylinux wheel
# bundles its own AlmaLinux-built shared libs (libpq/libpcre2/libssl/...); its
# vendored pcre2 10.32 is flagged CRITICAL by Trivy. Source build links the
# system libpq5 -> system libpcre2-8-0 (patched), eliminating the bundled CVEs.
# Do NOT switch back to psycopg2-binary without re-running the 10B.2 audit.
psycopg2>=2.9
redis>=5.0
rq>=1.16
# Pydantic v2 only. Upper-bounded to avoid a future v3 (or a stray old
# pydantic-settings) breaking `from pydantic_settings import BaseSettings`.
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
typer>=0.12
PyYAML>=6.0
# Phase 7L: keep yt-dlp fresh — an >90-day-old build logs "yt-dlp is older than
# 90 days" and YouTube extractors drift. Bump the floor and rebuild periodically.
yt-dlp>=2026.6.9
# Enables yt-dlp browser impersonation -> fewer "no impersonate target" warnings
# and 403/429s on YouTube. Optional but recommended in the container.
curl_cffi>=0.7
# YouTube Data API OAuth (Phase 6B). Only used when OAuth is explicitly enabled
# and configured; the app starts and runs fully without credentials. Imported
# lazily, so a missing/older version degrades to a classified "auth_required".
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
# Streaming JSON parser (Phase 6C). Used to iterate huge My Activity JSON
# arrays (90k+ watch / 11k+ liked) without loading the whole document into
# memory. Imported lazily; a missing version falls back to json.loads.
ijson>=3.2
